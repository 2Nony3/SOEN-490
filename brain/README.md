# Brain

The shared context/knowledge store for Operon. This is where FA Systems' operating knowledge lives — the single source of truth every agent will read from, so context is defined once instead of being re-explained to every tool.
